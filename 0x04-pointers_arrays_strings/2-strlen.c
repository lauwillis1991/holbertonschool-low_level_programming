#include "holberton.h"

/**
 * _strlen - returns the length of the string
 * @s: char type pointer.
 * Return: length of string
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; i[s] != '\0'; i++)

		;
	return (i);
}
