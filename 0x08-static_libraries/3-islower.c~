#include "holberton.h"

/**
 * _isdigit - Check if character is a digit.
 * @c: type int character
 * Return: 1 if digit, 0 otherwise
 */


int _islower(int c)

{
	if (c > 47 && c < 58)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
