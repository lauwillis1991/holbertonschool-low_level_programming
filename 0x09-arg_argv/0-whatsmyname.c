#include "holberton.h"
#include <stdio.h>

/**
 * main - print new name without compiling
 * @argc: arguement count - contains the number
 * of arguments passed to the program.
 * @argv: argument vector- 1d array of strings.
 * Return: 0;
 */

int main(int argc, char *argv[])
{

	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
