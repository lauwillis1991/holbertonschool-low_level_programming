#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)

{
	int start;

	for(start = 'a'; start <= 'z'; start++)
	{
		if (start == 'e')
		{
		continue;
		}
		if (start == 'q')
		{
		continue;
		}
		putchar(start);
	}
	putchar('\n');
	return (0);
}
